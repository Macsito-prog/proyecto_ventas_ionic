import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'punto_ventas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
